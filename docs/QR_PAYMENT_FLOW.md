# QR Payment Workflow - Visual Guide

Complete visual flow diagrams for the QR code payment confirmation system.

---

## Overview

```
Customer Orders → QR Generated → Customer Sees QR → Cashier Scans → Payment Confirmed
```

---

## 1. Order Creation & QR Generation

```
┌─────────────┐
│  Customer   │
│   Orders    │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────────────────┐
│  POST /api/orders                           │
│  ┌────────────────────────────────────────┐ │
│  │ Order Items, Customer Info, Table      │ │
│  └────────────────────────────────────────┘ │
└──────┬──────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  OrderService.createOrder()                  │
│  1. Create Order (status: PENDING)           │
│  2. Generate QR Token (JWT)                  │
│  3. Save to DB                               │
│  4. Broadcast via WebSocket                  │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  QRTokenService.generateToken()              │
│  ┌────────────────────────────────────────┐  │
│  │ JWT Token:                             │  │
│  │ - Token ID: qr_abc123                  │  │
│  │ - Short Code: QR2A3B4C                 │  │
│  │ - Order ID, Amount, Nonce              │  │
│  │ - Expires in 5 minutes                 │  │
│  └────────────────────────────────────────┘  │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  Database Tables Updated:                    │
│  ├─ orders (qr_token_id set)                 │
│  └─ payment_qr_tokens (new record)           │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  Response to Customer:                       │
│  {                                           │
│    "orderNumber": "ORD-2025-001",            │
│    "qrToken": "eyJhbGci...",                 │
│    "shortCode": "QR2A3B4C",                  │
│    "totalAmount": 13000                      │
│  }                                           │
└──────────────────────────────────────────────┘
```

---

## 2. Customer Receipt Display

```
┌────────────────────────────────────────┐
│   GARBAKING POS - Receipt              │
│   Order: ORD-2025-001                  │
│   Table: T-05                          │
│   ────────────────────────────────     │
│   2x Garbure Burger       11,000 XOF   │
│   1x French Fries          2,000 XOF   │
│   ────────────────────────────────     │
│   Total:                  13,000 XOF   │
│   ────────────────────────────────     │
│                                        │
│   ┌──────────────────────────────┐    │
│   │  ▄▄▄▄▄▄▄  ▄  ▄ ▄▄▄▄▄▄▄       │    │
│   │  █ ███ █ ▄█▀▄█ █ ███ █       │    │
│   │  █ ▀▀▀ █ ▀ ▀▀█ █ ▀▀▀ █       │    │
│   │  ▀▀▀▀▀▀▀ █ ▀ █ ▀▀▀▀▀▀▀       │    │
│   │  ▀█▀█ ▀▀▀█▄▄█▀▀▀ █▀█▀▀       │    │
│   │   ▀  █▀█ ▄ ▀██  ▄█ ▀▀█       │    │
│   │  ▄▄▄▄▄▄▄ █▀▄  █ ▀ ▀  █       │    │
│   │  █ ███ █  ▄▀█▀█▀▀█▀▀▀▀       │    │
│   │  █ ▀▀▀ █ ▀ █▀ █▄█▀█ ▀█       │    │
│   │  ▀▀▀▀▀▀▀ ▀ ▀  ▀ ▀▀ ▀ ▀       │    │
│   └──────────────────────────────┘    │
│                                        │
│   Scan to Pay                          │
│   ────────────────────────────────     │
│   Short Code: QR2A3B4C                 │
│   (Use if camera unavailable)          │
│   ────────────────────────────────     │
│   Expires: 14:30 (5 min)               │
└────────────────────────────────────────┘
```

---

## 3. Cashier Scan Workflow

### Option A: QR Code Scan

```
┌─────────────┐
│  Cashier    │
│  Scans QR   │
└──────┬──────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  POST /api/qr-payment/scan                   │
│  {                                           │
│    "qrToken": "eyJhbGci...",                 │
│    "deviceId": "POS-001"                     │
│  }                                           │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  QRPaymentService.scanQRCode()               │
│  1. Validate JWT signature                   │
│  2. Check token not expired                  │
│  3. Check token not already used             │
│  4. Verify nonce matches DB                  │
│  5. Check order not already paid             │
│  6. Log scan to audit table                  │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  ✅ SUCCESS Response:                        │
│  {                                           │
│    "success": true,                          │
│    "order": {                                │
│      "orderNumber": "ORD-2025-001",          │
│      "customerName": "John Doe",             │
│      "totalAmount": 13000,                   │
│      "items": [...]                          │
│    },                                        │
│    "tokenId": "qr_abc123",                   │
│    "shortCode": "QR2A3B4C"                   │
│  }                                           │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  POS Display Shows:                          │
│  ┌────────────────────────────────────────┐  │
│  │ Order: ORD-2025-001                    │  │
│  │ Customer: John Doe                     │  │
│  │ Table: T-05                            │  │
│  │                                        │  │
│  │ Items:                                 │  │
│  │ • 2x Garbure Burger    11,000 XOF      │  │
│  │ • 1x French Fries       2,000 XOF      │  │
│  │                                        │  │
│  │ Total: 13,000 XOF                      │  │
│  │                                        │  │
│  │ [Confirm Payment] [Cancel]             │  │
│  └────────────────────────────────────────┘  │
└──────────────────────────────────────────────┘
```

### Option B: Short Code Entry

```
┌─────────────┐
│  Cashier    │
│ Enters Code │
└──────┬──────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  Input: QR2A3B4C                             │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  POST /api/qr-payment/scan-short-code        │
│  {                                           │
│    "shortCode": "QR2A3B4C",                  │
│    "deviceId": "POS-001"                     │
│  }                                           │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  QRPaymentService.scanByShortCode()          │
│  1. Find token by short code                 │
│  2. Check not expired                        │
│  3. Check not already used                   │
│  4. Check order not already paid             │
│  5. Log scan to audit table                  │
└──────┬───────────────────────────────────────┘
       │
       ▼
    (Same success response as QR scan above)
```

---

## 4. Payment Confirmation

```
┌─────────────┐
│  Cashier    │
│  Confirms   │
└──────┬──────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  POST /api/qr-payment/confirm                │
│  {                                           │
│    "orderId": 123,                           │
│    "tokenId": "qr_abc123",                   │
│    "paymentMethod": "CASH",                  │
│    "transactionId": "TXN-2025-001",          │
│    "amountReceived": 15000,                  │
│    "deviceId": "POS-001"                     │
│  }                                           │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  QRPaymentService.confirmPayment()           │
│  1. Verify order exists                      │
│  2. Verify order not already paid            │
│  3. Mark token as USED                       │
│  4. Update order:                            │
│     - paymentStatus → PAID                   │
│     - status → CONFIRMED (if was PENDING)    │
│     - paidAt → now                           │
│     - qrPaymentConfirmedAt → now             │
│  5. Save to database                         │
│  6. Broadcast via WebSocket                  │
│  7. Log confirmation to audit table          │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  Database Updates:                           │
│  ├─ orders:                                  │
│  │   payment_status = PAID                   │
│  │   status = CONFIRMED                      │
│  │   paid_at = 2025-11-02 14:25:30           │
│  │   qr_payment_confirmed_at = now           │
│  │   qr_confirmed_by_user_id = 5             │
│  │   qr_confirmed_by_device_id = POS-001     │
│  │                                           │
│  └─ payment_qr_tokens:                       │
│      is_used = true                          │
│      used_at = 2025-11-02 14:25:30           │
│      used_by_user_id = 5                     │
│      used_by_device_id = POS-001             │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  WebSocket Broadcast:                        │
│  Topics:                                     │
│  ├─ /topic/orders/updated                    │
│  └─ /topic/orders/payment                    │
│                                              │
│  Payload:                                    │
│  {                                           │
│    "orderNumber": "ORD-2025-001",            │
│    "paymentStatus": "PAID",                  │
│    "status": "CONFIRMED"                     │
│  }                                           │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  ✅ SUCCESS Response:                        │
│  {                                           │
│    "success": true,                          │
│    "message": "Payment confirmed",           │
│    "order": {                                │
│      "paymentStatus": "PAID",                │
│      "status": "CONFIRMED",                  │
│      "paidAt": "2025-11-02T14:25:30"         │
│    }                                         │
│  }                                           │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  POS Shows Success:                          │
│  ┌────────────────────────────────────────┐  │
│  │ ✅ PAYMENT CONFIRMED                   │  │
│  │                                        │  │
│  │ Order: ORD-2025-001                    │  │
│  │ Amount: 13,000 XOF                     │  │
│  │ Method: CASH                           │  │
│  │ Received: 15,000 XOF                   │  │
│  │ Change: 2,000 XOF                      │  │
│  │                                        │  │
│  │ [Print Receipt] [New Order]            │  │
│  └────────────────────────────────────────┘  │
└──────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  Kitchen Display Updates:                    │
│  Order ORD-2025-001 → CONFIRMED → PREPARING  │
└──────────────────────────────────────────────┘
```

---

## 5. Error Scenarios

### A. Expired Token

```
Scan Attempt (6+ minutes after order)
       │
       ▼
┌──────────────────────────────────────────────┐
│  QRTokenService.validateToken()              │
│  ❌ JWT expiration check fails               │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  Response:                                   │
│  {                                           │
│    "success": false,                         │
│    "errorMessage": "Token expired",          │
│    "errorCode": "TOKEN_EXPIRED"              │
│  }                                           │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  POS Shows:                                  │
│  ⚠️  Token Expired                           │
│  Please ask customer to regenerate QR code   │
│                                              │
│  [Regenerate QR] [Cancel]                    │
└──────────────────────────────────────────────┘
```

### B. Already Paid Order

```
Second Scan Attempt (after payment confirmed)
       │
       ▼
┌──────────────────────────────────────────────┐
│  QRPaymentService.scanQRCode()               │
│  ❌ Order payment_status == PAID             │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  Response:                                   │
│  {                                           │
│    "success": false,                         │
│    "errorMessage": "Order already paid",     │
│    "errorCode": "ORDER_ALREADY_PAID"         │
│  }                                           │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  POS Shows:                                  │
│  ℹ️  Order Already Paid                      │
│  This order has been paid for.               │
│                                              │
│  [OK]                                        │
└──────────────────────────────────────────────┘
```

### C. Duplicate Confirmation

```
Second Confirm Attempt (same token)
       │
       ▼
┌──────────────────────────────────────────────┐
│  QRTokenService.markTokenUsed()              │
│  ❌ Token is_used == true                    │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  Response:                                   │
│  {                                           │
│    "success": false,                         │
│    "errorMessage": "Token already used",     │
│    "errorCode": "CONFIRMATION_FAILED"        │
│  }                                           │
└──────────────────────────────────────────────┘
```

### D. Replay Attack

```
Reused QR Token (different nonce)
       │
       ▼
┌──────────────────────────────────────────────┐
│  QRTokenService.validateToken()              │
│  ❌ Nonce mismatch detected                  │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  Response:                                   │
│  {                                           │
│    "success": false,                         │
│    "errorMessage": "Nonce mismatch",         │
│    "errorCode": "REPLAY_ATTACK"              │
│  }                                           │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  🚨 Security Alert Logged                    │
│  Admin notified of potential replay attack   │
└──────────────────────────────────────────────┘
```

---

## 6. Rate Limiting

```
10+ Scan Requests in 1 Minute
       │
       ▼
┌──────────────────────────────────────────────┐
│  Bucket4j Rate Limiter                       │
│  ❌ Bucket.tryConsume(1) → false             │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  HTTP 429 Too Many Requests:                 │
│  {                                           │
│    "success": false,                         │
│    "errorMessage": "Too many scan attempts", │
│    "errorCode": "RATE_LIMIT_EXCEEDED"        │
│  }                                           │
└──────┬───────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────┐
│  POS Shows:                                  │
│  ⚠️  Rate Limit Exceeded                     │
│  Please wait 60 seconds before trying again  │
│                                              │
│  [OK]                                        │
└──────────────────────────────────────────────┘
```

---

## 7. Database Schema Flow

```
┌─────────────────────────────────────────────┐
│  orders                                     │
│  ─────────────────────────────────────────  │
│  id                 BIGINT PK               │
│  order_number       VARCHAR                 │
│  status             ENUM (PENDING → ...)    │
│  payment_status     ENUM (PENDING → PAID)   │
│  qr_token_id        VARCHAR ────────┐       │
│  qr_payment_confirmed_at  TIMESTAMP │       │
│  qr_confirmed_by_user_id  BIGINT    │       │
│  qr_confirmed_by_device_id VARCHAR  │       │
│  paid_at            TIMESTAMP       │       │
└─────────────────────────────────────┼───────┘
                                      │
                                      │ FK
                                      ▼
┌─────────────────────────────────────────────┐
│  payment_qr_tokens                          │
│  ─────────────────────────────────────────  │
│  id                 BIGINT PK               │
│  token_id           VARCHAR UNIQUE          │
│  order_id           BIGINT FK               │
│  nonce              VARCHAR UNIQUE          │
│  short_code         VARCHAR(8) UNIQUE       │
│  token_hash         VARCHAR(255)            │
│  issued_at          TIMESTAMP               │
│  expires_at         TIMESTAMP               │
│  is_used            BOOLEAN DEFAULT false   │
│  used_at            TIMESTAMP               │
│  used_by_user_id    BIGINT                  │
│  used_by_device_id  VARCHAR                 │
└─────────────────────────────────────────────┘
                   │
                   │ Audit Trail
                   ▼
┌─────────────────────────────────────────────┐
│  qr_scan_audit_logs                         │
│  ─────────────────────────────────────────  │
│  id                 BIGINT PK               │
│  order_id           BIGINT                  │
│  token_id           VARCHAR                 │
│  short_code         VARCHAR                 │
│  action             VARCHAR (SCAN/CONFIRM)  │
│  status             VARCHAR (SUCCESS/FAIL)  │
│  error_message      TEXT                    │
│  device_id          VARCHAR                 │
│  user_id            BIGINT                  │
│  scan_timestamp     TIMESTAMP               │
│  processing_time_ms INT                     │
│  payment_method     VARCHAR                 │
│  payment_amount     DECIMAL                 │
└─────────────────────────────────────────────┘
```

---

## 8. Complete End-to-End Timeline

```
Time    Event                           Database State
─────   ────────────────────────────    ──────────────────────────────
14:20   Customer orders
        → Order created                 orders: status=PENDING
                                        payment_status=PENDING

14:20   QR token auto-generated         payment_qr_tokens: new record
        → Token saved                   orders: qr_token_id set

14:20   Customer receives receipt       (no DB changes)
        → Shows QR code + short code

14:22   Cashier scans QR                qr_scan_audit_logs: SCAN/SUCCESS
        → Validates & retrieves order   (no updates, read-only)

14:23   Cashier confirms payment        payment_qr_tokens: is_used=true
        → Token marked used             orders: payment_status=PAID
        → Order updated                          status=CONFIRMED
        → WebSocket broadcast           qr_scan_audit_logs: CONFIRM/SUCCESS

14:24   Kitchen receives order          (handled by order workflow)
        → Starts preparing

14:30   Token expires                   (automatic, time-based)
        → No further scans possible

14:35   Customer tries to scan again    qr_scan_audit_logs: SCAN/DUPLICATE
        → Rejected (already paid)       (no updates)
```

---

## Summary

### ✅ Secure Flow
1. JWT signed with HMAC-SHA256
2. 5-minute expiration window
3. Nonce prevents replay attacks
4. One-time use enforcement
5. Rate limiting prevents abuse
6. Comprehensive audit logging

### ✅ User-Friendly
1. QR code for fast scanning
2. Short code fallback (8 chars)
3. Clear error messages
4. Real-time WebSocket updates
5. Works offline (queue for sync)

### ✅ Robust
1. Duplicate prevention
2. Already-paid detection
3. Token expiration handling
4. Device tracking
5. Transaction idempotency
6. Analytics & monitoring

---

**Last Updated:** 2025-11-02
**Workflow Version:** 1.0
